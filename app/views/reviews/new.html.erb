<div class="container">
  <h2>Create strategy review</h2>
  <div class="row">
    <div class="col-sm-10">
      <%= simple_form_for [@share, @review] do |f| %>
        <%= f.input :investment_strategy %>
        <%= f.input :current_strategy %>
        <%= f.label :preconisation_id %><br />
        <%= f.select :preconisation_id, options_for_select([['Aucune',],['Vendre',1],['Alléger',2],['Neutre',3],['Conserver',4],['Acheter',5]]), class:"form-control" %>
        <%= f.submit "save the review", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
